# Write your code here
def make_path(parts):
    return "/".join(parts)

    # path = ""
    # count = 0
    # for p in parts:
    #     if count != len(parts)-1:
    #         path += f"{p}/"
    #         count +=1
    #     else:
    #         path += f"{p}"
    # return path


